// Used to print the data in a formatted way
const commonConsole = (data, message) => {
    console.log("\n-----------------------------------");
    console.log(data, message);
    console.log("-----------------------------------\n");
}

module.exports = commonConsole;